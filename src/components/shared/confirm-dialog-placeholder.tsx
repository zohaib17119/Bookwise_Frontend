import { Alert } from "@/components/ui/alert";

export function ConfirmDialogPlaceholder() {
  return (
    <Alert
      title="Confirm dialog foundation"
      description="Use this slot when delete/archive actions are introduced for company-aware accounting resources."
    />
  );
}
